> Convert Fields and Funcs

python 3 required

# Replace all fields and funcs in one file.
python replace.py <file> (-r -> reverse)

# Replace all fields and funcs in the whole directory.
python replace_all.py <directory> (-r -> reverse)
